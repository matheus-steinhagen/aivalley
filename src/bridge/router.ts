import type { RouteConfig } from '../type'

export const router: Record<string, RouteConfig> = {
	'/': {
		layout: 'main',
		title: "Vale das IAs",
		description: "Estudo de interação de inteligências",
		keywords: "ia, interação, estudo, vale"
	},
}